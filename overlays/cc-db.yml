#@data/values
---
images:
  capi: gcr.io/peripli/cloud-controller-ng@sha256:85204b994c22702d6e868f5f61440de3aa61f1abdc7c89b4e0fb57171bd84173
  
#@ if hasattr(self,"capi_db_credentials"):
capi:
  database:
    adapter: postgres
    host: #@ self.capi_db_credentials.host
    port: #@ self.capi_db_credentials.port
    user: #@ self.capi_db_credentials.user
    password: #@ self.capi_db_credentials.password
    name: #@ self.capi_db_credentials.database
#@ end
#@ if hasattr(self,"uaa_db_credentials"):
uaa:
  database:
    adapter: postgresql
    host: #@ self.uaa_db_credentials.host
    port: #@ self.uaa_db_credentials.port
    user: #@ self.uaa_db_credentials.user
    password: #@ self.uaa_db_credentials.password
    name: #@ self.uaa_db_credentials.database
#@ end
